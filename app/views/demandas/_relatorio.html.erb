<%= form_for(relatorio_colaborador) do |f| %>
  <% if relatorio_colaborador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relatorio_colaborador.errors.count, "error") %> prohibited this relatorio_colaborador from being saved:</h2>

      <ul>
      <% relatorio_colaborador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<div class="card">
  <div class="card-content">
    <span class="card-title">Novo relatório</span>
      <div class="input-field">
        <%= f.label :descricao %>
        <%= f.text_area :descricao, :class => "materialize-textarea" %>
      </div>
  </div>
  <div class="card-action">
      <%= f.submit 'Enviar', :class => 'btn cyan' %>
  </div>
</div>

  <% f.object.demanda_id = params[:id]%>
  <%= f.hidden_field :demanda_id%>
  <% f.object.colaborador_id = current_user.id%>
  <%= f.hidden_field :colaborador_id%>


<% end %>
